import { JsonRefsOptions } from "json-refs";
import { Options, JsonSchema } from "./Types";
export declare const jsonSchemaToZodDereffed: (
  schema: JsonSchema,
  options?: Options & {
    jsonRefsOptions?: JsonRefsOptions;
  }
) => Promise<string>;
export declare const jsonSchemaToZod: (
  schema: JsonSchema,
  { module, name, ...rest }?: Options
) => string;
